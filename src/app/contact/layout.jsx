export const metadata = {
    title: "Contact Us | Skyradix",
    description: "Get in touch with Skyradix. Whether you have a fully scoped project or just a raw concept, let's build the right system for it."
};

export default function ContactLayout({ children }) {
    return children;
}
